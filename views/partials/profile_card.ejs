<div class="position-absolute top-0 start-100 translate-middle p-2 bg-light" style="width: fit-content">
  <div
    class="card d-flex flex-sm-row align-items-center p-2 shadow profile-card text-nowrap"
    style="border-radius: 0.5rem">
    <i class="fa fa-user-circle fa-2x" aria-hidden="true"></i>
    <div class="ps-2 d-flex flex-sm-column">
      <p class="h6 mb-0">
        <%= user.username %>
      </p>
      <p class="h6 mb-0 fw-lighter">
        <% if (user.username === "admin") {%>
          Sở VH-TT
        <% } else if (user.position === 1) { %>
          Cán bộ Quận
        <% } else { %>
          Cán bộ Phường
        <% } %>
      </p>
    </div>
  </div>
</div>
